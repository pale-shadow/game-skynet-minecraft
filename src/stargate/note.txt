To integrate the redstone telemetry from the Chonk host into the Stargate control plane, you should modify the existing `stargate-daemon.py` to include an asynchronous listener and reorganize the directory structure to support scalable event handling.

---

## 1. Directory Restructuring
Refactor the `src/servers/stargate` directory to decouple the network listener from the MCP logic. This prevents the `stargate-daemon.py` from becoming a monolithic bottleneck.

* **`src/servers/stargate/internal/telemetry/`**: Create this directory for the socket server logic and packet validation.
* **`src/servers/stargate/handlers/`**: Create this directory to house specific callbacks for events like `CART_PASS`.
* **`src/servers/stargate/telemetry_listener.py`**: A new module to handle the raw TCP/UDP ingestion from the Chonk bridge.

## 2. Refactoring `stargate-daemon.py`
The current daemon likely focuses on managing the RCON connection and schematic injection. You must extend it to host an asynchronous server that listens for telemetry packets forwarded by the Chonk host.

**Core Updates:**
* **Async Socket Server**: Implement an `asyncio` base server within the daemon. It should bind to a specific port (e.g., 5005) to receive JSON payloads from the Chonk host's bridge script.
* **Event Dispatcher**: Add a dispatcher that maps incoming `EVENT` types to functions in the `handlers/` directory. For the minecart signal, map `CART_PASS` to a function that triggers the necessary MCP world-state updates.

## 3. Service Configuration Updates
Update `stargate-daemon.service` to include the necessary networking environment variables for the new bridge.

**Modified `[Service]` Section:**
```ini
[Service]
User=franklin
WorkingDirectory=/mnt/clusterfs2/workspace/gaming/game-skynet-minecraft
# Add TELEMETRY_PORT and CHONK_IP for filtering
Environment="RCON_PASS=dinosaurExTraVaGanZa1969" "RCON_PORT=25575" "TELEMETRY_PORT=5005" "CHONK_IP=192.168.1.50"
ExecStart=/mnt/clusterfs2/workspace/gaming/game-skynet-minecraft/.venv/bin/python3 src/servers/stargate/stargate-daemon.py
```

## 4. MCP Integration (Stargate Host)
Since Stargate is the primary MCP node, the `CART_PASS` handler should directly interface with the Model Context Protocol tools.

* **Logic Implementation**: When the minecart triggers the detector on Chonk, the Stargate handler can execute a `clear_zone` or `place_schematic` command back to the Chonk host via the existing RCON link.
* **Latency Management**: Ensure the telemetry listener uses non-blocking I/O. If the MCP task involves heavy AI inference (e.g., generating a new structure mutation), the handler should offload the task to a task queue to avoid stalling the telemetry stream.

## 5. Deployment and Verification
After updating the code, use the existing `deploy.sh` to restart the service and run the diagnostic suite.

* **`test_mcp_stargate.py`**: Verify that the Stargate host can receive a simulated JSON packet and successfully translate it into an MCP command.
* **`stargate-daemon.log`**: Monitor the log for "Telemetry received from CHONK-01" entries.
* **Boundary Checks**: Run `test_schematic_boundary_safety.py` to ensure that any automated building triggered by the minecart signal stays within the allocated coordinates on the Chonk host.

This approach preserves the existing infrastructure while turning the Stargate host into a reactive control hub for events originating on the Chonk node.
