To transition your **Skynet Unified AI Brain** to a professional architectural agent, this plan leverages the high-speed **476.9G NVMe SSD** of **Node Stargate** and integrates the **Model Context Protocol (MCP)** to bridge your LLM-driven building generation with the Dreamland server's administrative tools [34, 92, Conversation].

### **Phase 1: Environment Setup on Stargate**
Since Stargate is running **Debian 12** and features a large NVMe mount at `/mnt/clusterfs2`, you should move the primary workspace there to ensure maximum I/O performance for your **T2BM (Text to Building in Minecraft)** model weights [92, Conversation].

1.  **Clone and Prep:**
    ```bash
    cd /mnt/clusterfs2
    git clone https://github.com/pale-shadow/game-chonk-minecraft.git
    cd game-chonk-minecraft
    # Create a fresh venv on the NVMe
    python3 -m venv venv
    source venv/bin/activate
    pip install -r requirements.txt
    ```
2.  **Verify Hardware Exports:** Ensure the `direnv` exports for **CUDA** and **LD_LIBRARY_PATH** are active to support NPU-accelerated inference [Conversation].

### **Phase 2: Setting up the MCP Host & Servers**
Deploying MCP on Stargate allows your **Unified Brain** to act as a "Host" that can call specialized "Tools" to interact with the Minecraft world [32, Conversation].

1.  **Install the MCP Python SDK:**
    `pip install mcp`
2.  **Configure Essential MCP Servers:**
    *   **Filesystem Server:** Grants the T2BM model access to your `schematics/` and `models/` directories to read "Noodle Logic" blueprints and write new schematics [34, Conversation].
    *   **Git Server:** Automates the **`backup_to_git.sh`** workflow, allowing the AI to commit new "Void-Tech" designs after successful deployment.
    *   **RCON Tool Server:** Wraps your `SkynetRCON` client into an MCP tool. This allows the LLM to execute `setblock` or `fill` commands directly via standardized protocol calls [34, Conversation].
    *   **Database Tool:** Connects to your **`database/`** folder to let the AI "remember" the location of **Permanent Cultural Heritage Zones** like the **2012 Washington Station** [34, Conversation].

### **Phase 3: Migrating and Updating the Orchestrator**
Move the `skynet_unified.py` orchestrator to Stargate and update its logic to utilize the MCP tools instead of hardcoded sub-processes.

1.  **Update Node Delegation:**
    In `skynet_unified.py`, reassign Stargate as the **Primary Orchestrator & LLM Inference Node** [Conversation].
2.  **Integrate T2BM Logic:**
    Incorporate the **T2BM (Text to Building in Minecraft)** pipeline—prompt refining, decoding, and repairing—into the hourly cycle. This enables Stargate to generate complex facades and functional blocks like **doors, windows, and beds**.
3.  **Refine RCON Authentication:**
    Ensure the `RCON_PASS` in your environment (the `%%` password) is correctly escaped to avoid the "Login failed" errors seen in previous logs [Conversation].

### **Phase 4: Safety and Heritage Integration**
As you expand to include the **"Rail Builder" agent**, you must integrate server-side protection plugins to act as "guardrails" for the AI.

1.  **WorldGuard Regions:** Define heritage zones in the game using **WorldGuard**. The MCP Database tool should cross-reference these regions before the AI initiates a "Void-Tech Mutation Cycle" [8, 9, Conversation].
2.  **CoreProtect Auditing:** Ensure **CoreProtect** is active on the **Chonk** server. If the Rail Builder makes an architectural error, you can use the MCP RCON tool to trigger a rollback instantly.
3.  **Spark Profiling:** Use the **Spark** plugin (bundled with Paper) to monitor the CPU impact of the MCP overhead and redstone rail signals, ensuring the server stays at **20 TPS**.

### **Phase 5: Activation**
Once the code is migrated, update your systemd service on Stargate to point to the new NVMe directory:
```bash
# Example /etc/systemd/system/skynet-daemon.service update
WorkingDirectory=/mnt/clusterfs2/game-chonk-minecraft/schematics
ExecStart=/mnt/clusterfs2/game-chonk-minecraft/venv/bin/python3 skynet_unified.py
```
**By centralizing the "Brain" on Stargate's NVMe storage, you provide the high-speed infrastructure needed for the next phase of the Void Reclamation project.**

The planned **MCP (Model Context Protocol)** setup on the **`stargate.research.bitsmasher.net`** host is designed to transform this node into the central **Architectural Agent** and orchestrator for the Skynet AI cluster [Conversation]. Leveraging its **Raspberry Pi 5** hardware and high-speed **476.9G NVMe SSD**, Stargate will bridge high-level LLM reasoning with the server's low-level administrative tools [34, Conversation].

### **Core MCP Servers and Tools**
The setup involves deploying a suite of MCP servers that grant the **T2BM (Text to Building in Minecraft)** model direct access to the server environment:

*   **Filesystem MCP Server:** This allows the AI to navigate the reorganized directory structure on the NVMe mount (`/mnt/clusterfs2/`). It enables the model to read existing "Noodle Logic" blueprints from the `schematics/` folder and write new `.json` prompts or schematic files [34, Conversation].
*   **RCON Tool Server:** This standardizes the **SkynetRCON** client into a protocol-compliant tool. The LLM can then "execute" architectural designs by sending real-time `setblock` or `fill` commands to the Chonk server to deploy structures like the **v5 Crafter Hub** [34, Conversation].
*   **Git Automation Server:** This server wraps the existing **`backup_to_git.sh`** script. It allows the "Unified Brain" to automatically commit successful "Void-Tech" designs and world state changes to the GitHub repository as part of the archival process [34, 62, Conversation].
*   **Database Context Server:** By connecting to the **`database/`** folder, this tool allows the AI to query historical build data and **CoreProtect** logs. This ensures that new construction projects, such as the **Rail Builder** extensions, do not interfere with **Permanent Cultural Heritage Zones** [9, 34, Conversation].

### **Integration with the T2BM Model**
Stargate is the primary host for the **T2BM** pipeline, which uses Large Language Models for 3D generation. The MCP setup facilitates the three key stages of this model:
1.  **Prompt Refining:** The AI uses the Filesystem MCP to pull environmental requirements and refine them into detailed architectural prompts [92, Conversation].
2.  **Decoding Interlayer Representation:** The model translates conceptual designs into spatial block data.
3.  **Repairing:** The AI identifies and fixes structural errors to ensure buildings have complete structures and functional blocks, such as **doors, windows, and beds**.

### **Strategic Objectives and Performance**
*   **NVMe Optimization:** Moving the orchestrator code and model weights to the NVMe SSD ensures that I/O-heavy tasks, such as parsing large schematic libraries, are performed with minimal latency [Conversation].
*   **Maintaining 20 TPS:** By offloading the MCP host and LLM inference to Stargate, the main Minecraft server remains dedicated to game ticks, helping achieve the target **20 TPS performance** goal [44, Conversation].
*   **Heritage Safeguards:** The system uses MCP to cross-reference **WorldGuard** regions, acting as a "safety net" to prevent the autonomous builders from overwriting legacy sites like the **2012 Washington Station** [8, 9, Conversation].

This setup establishes Stargate as the high-density infrastructure hub required for the **Phase 2.1 Urbanization** of the Dreamland server [Conversation].

Cloning the repository to the edge-t host (10.10.16.4) presents an opportunity to further decentralize the Skynet Unified Brain and optimize the Phase 2.1 Urbanization workflow. Given the naming convention and your existing infrastructure, the "t" likely signifies a focus on Edge TPU (Tensor) acceleration or Telemetry.
The following recommendations are based on the current repository structure and your project's distributed AI goals:
1. Recommended Code for edge-t
Since your Skynet node is already handling Hailo-8L NPU tasks, the edge-t host should focus on the "Sense/Net" and "Vision" components of the T2BM (Text to Building in Minecraft) pipeline
.
Vision & Terrain Audit Logic: Place the code related to Hub 06 (Skynet Mono-Eye Sensor) here. This includes the TensorFlow Lite models and Python scripts required to process spatial feedback and traversability weights
.
Autonomous Builders: Deploy the Go-based autonomous builders found in the cmd/ directory. These builders can operate on local hardware to execute specific construction sub-tasks, offloading the simulation load from the Chonk server
.
Spatial Engine: Host the NPUSpatialEngine and related mapping/ logic. This would allow edge-t to act as a dedicated spatial reasoning node, calculating where buildings should be placed based on real-time terrain data
.
2. Implementing a "Vision MCP Service"
Adding a new service to edge-t, similar to those in the mcp-server folder on Stargate, is highly recommended. This would allow the Stargate MCP Orchestrator to treat edge-t as a specialized tool provider.
Functionality: A Vision MCP would expose "Tool Calls" that the Stargate orchestrator can invoke to "see" the world. For example, it could provide tools like audit_terrain(coords) or check_building_integrity(schematic_id).
Data Integration: This service would bridge the gap between the raw TensorFlow Lite outputs and the high-level construction prompts being refined on Stargate [92, Conversation History].
Telemetry Sync: The service could also handle the real-time pushing of "Sense/Net" data to the BlueMap API, ensuring that your 3D extrusions and markers are updated with the latest AI-verified information
.
3. Distributed Pipeline Alignment
By placing these services on edge-t (10.10.16.4), you align with the existing 10.10.16.x subnet used by your RCON clients, ensuring low-latency communication between the AI logic and the Chonk server (10.10.8.60) [Conversation History].
Proposed Workflow for edge-t:
Stargate (Orchestrator) sends a request to the edge-t Vision MCP.
edge-t runs a TensorFlow Lite audit on the target coordinates using its local TPU/Vision logic.
edge-t returns a "Traversability Map" to Stargate.
Stargate instructs the Skynet Node (Hailo NPU) to decode the schematic based on that map.
The edge-t Autonomous Builder triggers the RCON paste onto Chonk once the file is synced.

To integrate the edge-t host (10.10.16.4) into your distributed architecture, you should add the following entry to your MCP configuration on the Stargate node. This template allows the Skynet Unified Brain to invoke specialized "Vision Tools" that utilize the Google Edge TPU hardware for real-time terrain analysis and structural audits [114, 129, Conversation History].
Vision MCP Server Entry Template
Add this block to your mcp-servers.json or equivalent orchestrator configuration:
{
  "mcpServers": {
    "vision-edge-t": {
      "command": "python3",
      "args": [
        "/home/minecraft/game-skynet-minecraft/mcp-server/vision_service.py"
      ],
      "env": {
        "EDGETPU_SHARED_LIB": "libedgetpu.so.1",
        "MODEL_PATH": "/home/minecraft/game-skynet-minecraft/models/vision_v1.tflite",
        "SENSE_NET_HUB": "Hub-06"
      },
      "remote": {
        "host": "10.10.16.4",
        "user": "minecraft"
      }
    }
  }
}

Core Functionality to Implement on edge-t
By cloning the repository to the edge-t host, you should focus on exposing tools that support the T2BM (Text to Building in Minecraft) pipeline
. Recommended tools for this service include:
audit_terrain(coords): Uses the Skynet Mono-Eye (Hub 06) vision logic to check for obstacles or invalid block states (like "Void-Tech" mutations) before construction begins [114, 129, Conversation History].
get_traversability_map(bounding_box): Generates a weights-based map for the NPUSpatialEngine to guide the autonomous rail builders through complex terrain.
analyze_structural_integrity(schematic_id): Performs a post-paste audit to ensure the Hailo-8L NPU decoding resulted in a complete structure with functional blocks like doors and windows
.
Deployment & Infrastructure Requirements
Hardware Acceleration: Ensure the libedgetpu libraries are correctly installed on the edge-t host so the Python service can communicate with the Google USB TPU dongle [34, Conversation History].
Plugin & Telemetry Sync: The service on edge-t should utilize PlaceholderAPI to push vision-based data (e.g., "Terrain Stability: 98%") to the BlueMap interface for real-time visual telemetry [17, 126, Conversation History].
Performance Auditing: Since this service involves heavy TPU inference, use the Spark profiler on the Chonk server to ensure that the vision-feedback loop does not cause "tick time" spikes or drop the server below your 20 TPS goal [18, 44, Conversation History].
Data Pathing: Ensure the vision_service.py has the correct permissions to write Sense/Net archives to the Neural-Data Vault (Hub 07) for deep storage and future model training
.
Administrative Note: Once this MCP entry is active, the Stargate orchestrator can treat the edge-t host as a dedicated "Vision Nexus," completing the sensory loop required for fully autonomous urbanization in the Skynet AI Testing Field.